# CRV — package.json declara engines.node >=22. Sin engine-strict, npm solo
# advierte y sigue instalando con Node 20, que es exactamente el fallo que
# F0 detectó en esta máquina (ver docs/PHASES.md, nota de entorno).
engine-strict=true
